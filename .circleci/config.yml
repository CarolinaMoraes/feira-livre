version: "2.1"
orbs:
  node: circleci/node@5.0.3
jobs:
  setup:
    docker:
      - image: "cimg/base:stable"
    steps:
      - checkout
      - node/install:
          node-version: "18.12.1"
      - run: node --version
workflows:
  ci:
    jobs:
      - setup
